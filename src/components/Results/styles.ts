import styled from "styled-components";
import { Badge, Box } from "@cruk/cruk-react-components"

export const StyledResultsBox = styled(Box)`
  display: flex;
  justify-content: space-between;
`;

export const StyledBadge = styled(Badge)`
margin: 0 4px;
`